import styled from "styled-components";

export const HomeWrapper = styled.div`
  .roomBox {
    max-width: 1080px;
    margin: 0 auto;
    box-sizing: border-box;
    padding: 0px 24px;
    margin-top: 40px;
  }
`;
