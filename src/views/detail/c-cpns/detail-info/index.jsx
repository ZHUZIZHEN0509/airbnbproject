import React, { memo } from "react";
import { DetailInfoWrapper } from "./style";

const DetailInfo = memo(() => {
  return <DetailInfoWrapper>index</DetailInfoWrapper>;
});

export default DetailInfo;
